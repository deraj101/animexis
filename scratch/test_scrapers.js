const scraperService = require('../src/services/scraperService'); async function testSingleProvider(name, query) { console.log(\\\n--- Testing Provider: \ ---\); try { const searchResults = await scraperService.searchAnime(query, 1, name); console.log(\Search for \\\
\\\\:\, searchResults.success ? \Found \ items\ : 'Failed'); if (searchResults.success && searchResults.results.length > 0) { const firstResult = searchResults.results[0]; const id = firstResult.slug || firstResult.id; console.log(\Getting details for: \ (\)\); const details = await scraperService.getAnimeDetails(id, name); console.log(\Details:\, details.success ? \Found \ episodes\ : 'Failed'); } } catch (error) { console.error(\Error testing \:\, error.message); } } async function runTests() { await testSingleProvider('gogoanime', 'Naruto'); await testSingleProvider('kaido', 'One Piece'); await testSingleProvider('animepahe', 'Bleach'); } runTests();
