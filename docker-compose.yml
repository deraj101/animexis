services:
  mongodb:
    image: mongo:latest
    container_name: animexis-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

  redis:
    image: redis:latest
    container_name: animexis-redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  mongo-data:
